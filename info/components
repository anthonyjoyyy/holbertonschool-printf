_printf - main function

flag - stuct for storing flags
finit - initializer for flag stuct
sinit - initialize a string to ' ' and terminate

dflags - detect flags from given string and update a flag struct, returning new string position
dwidth - detect width in given string and save to flag struct, returning new string pos
specifier - execute the aptopiate fuction given a char, and pass flag struct and va_arg

prntchar - print a single given char
prntc - print a singe char formated with flags and width
prnts - print a string with width and flags

yet to make:
dper:
	take flag struct, string, and string pos
	return new string pos
	int (struct flag*, char *, int)
func for %d:
	print int from varg
	suported flags:
	{'0','+','-',' ','1..9'}
	in-progress prototype:
	printc(flag *, va_arg(varg, int));
func for %f:
	print float form varg
	suported flags:
	{'0','+','-',' ','1..9','.'}
	in-progress prototype:
	printc(flag *, va_arg(varg, float));
