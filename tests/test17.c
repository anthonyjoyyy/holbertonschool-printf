#include <limits.h>
#include "../main.h"

/**
 * main - Entry point
 *
 * Return: Always 0
 */
int main(void)
{
	_printf("testing multible paramaters:[one: %s\ttwo: %s]\n", "hello", "world");
	printf("testing multiple parameters:[one: %s\ttwo: %s]\n", "hello", "world");
    return (0);
}
