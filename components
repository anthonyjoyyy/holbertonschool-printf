_printf - main function
flag - stuct for storing flags
finit - initializer for flag stuct
inttostr - function for converting ints to strings
printchar - print a single given char
dflags - detect flags from given string and update a flag struct, returning new string position

detectwidth:
	take flag stuct, string, and string pos
	return new string pos
	int (struct flag*, char *, int)

detectper:
	take flag struct, string, and string pos
	return new string pos
	int (struct flag*, char *, int)
